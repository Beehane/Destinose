<div class="container text-center">

<h4 class="text-center">it looks like you'd  <strong><i>love</i></strong></h4>
<h3 class="text-center"><%= @country %></h3>
<p>

<p class="text-center"><%= link_to "save this result", save_result_path(centroid: @centroid, country: @country) %> | <%= link_to 'keep swiping', next_card_path %></p>
<p class="text-center">round-trip flights from <a href="<%= @skyscanner_url %>"><%= @quote.round %>&euro;</a></p>
<%= render 'recommendation' %>

  <div class="bottom-wrapper">
    <div class="bottom-container">
      <div class="bottom-msg-left">Out of <%= @liked.count %> images you liked, <%= @cluster[0].count %> of them were in <%= @country %>. <% if @cluster.key?(1) %>You also really liked <%= @country_b %><% end %><% if @cluster.key?(2) %> and <%= @country_c %>, too<% end %>
      </div>
    </div>
  </div>

</div>

