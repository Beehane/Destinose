<center>
<h1>Destinosé</h1>
<%= cl_image_tag(@current_card.image, height: 500, width: 500, crop: :fill, id: @current_card.id)%>
<p>image ID: <%= @current_card.id %></p>
<table>
<tr>
  <td><%= link_to "No", swipe_path(liked: 0, card_id: @current_card.id) %></td>
  <td><%= link_to "Yes", swipe_path(liked: 1, card_id: @current_card.id) %></td>
</tr>
</table>

<p style="float: right; padding-right: 50px;">Current Strength: <%= @current_strength %>&percnt;</p>

</center>
