<center>
<h1>Destinosé</h1>
<%= cl_image_tag(@new_card.image, height: 500, width: 500, crop: :fill)%>
<table>
<tr>
  <td><%= link_to "No", swipe_path(liked: 0, card_id: @new_card.id) %></td>
  <td><%= link_to "Yes", swipe_path(liked: 1, card_id: @new_card.id) %></td>
</tr>
</table>

<p style="float: right; padding-right: 50px;">Current Strength: XX%</p>

</center>
